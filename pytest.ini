[pytest]
pythonpath = .
testpaths = tests
strict_markers = true
markers =
    stage1: create a link + redirect + 404/422 basics
    stage2: click counting + stats
    stage3: custom aliases + 409 collisions
    stage4: edit (PATCH) + delete (DELETE)
    stage5: link expiration (410 Gone)
